When 3x3 was run in the stratosphere with u,v,theta, and w, the training loss did not converge, even though the 5x5 model when run using 2 3x3 CNNs showed fast convergence. 

So, maybe the 3x3 CNN should be run with a lower learning rate to avoid error spikes
